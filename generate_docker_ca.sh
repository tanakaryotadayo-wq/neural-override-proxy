#!/bin/bash
set -e

echo "🔑 Generating self-signed CA for mitmproxy..."
# TODO: Implement openssl commands or mitmproxy CA generation
# e.g., using mitmproxy to generate the initial CA certs to a mounted volume

echo "✅ CA generated."
